apiVersion: apps/v1
kind: Deployment
metadata:
  name: o2obots-new-frontend
  namespace: application
spec:
  replicas: 1
  selector:
    matchLabels:
      app: o2obots-new-frontend
  template:
    metadata:
      labels:
        app: o2obots-new-frontend
        env: dev
    spec:
      serviceAccountName: o2obots-default-app
      containers:
        - name: o2obots-new-frontend
          image: o2obotsteam/o2obots-new-frontend-dev:CHANGE_ME
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5000
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 500m
              memory: 512Mi
      imagePullSecrets:
        - name: o2obots-docker
---
apiVersion: v1
kind: Service
metadata:
  name: o2obots-new-frontend
  namespace: application
spec:
  type: ClusterIP
  selector:
    app: o2obots-new-frontend
  ports:
    - port: 80
      targetPort: 5000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: o2obots-new-frontend
  namespace: application
  annotations:
    alb.ingress.kubernetes.io/group.name: o2obots-private-dev
    alb.ingress.kubernetes.io/scheme: internal
    alb.ingress.kubernetes.io/ssl-redirect: '443'
    alb.ingress.kubernetes.io/healthcheck-interval-seconds: '30'
    alb.ingress.kubernetes.io/load-balancer-attributes: idle_timeout.timeout_seconds=300
    alb.ingress.kubernetes.io/healthcheck-path: /
    alb.ingress.kubernetes.io/healthcheck-protocol: HTTP
    alb.ingress.kubernetes.io/healthcheck-timeout-seconds: '5'
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}, {"HTTPS": 443}]'
    external-dns.alpha.kubernetes.io/hostname: app.dev.o2ospot.com
spec:
  ingressClassName: alb
  rules:
    - host: app.dev.o2ospot.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: o2obots-new-frontend
                port:
                  number: 80
# ---
# apiVersion: autoscaling/v2
# kind: HorizontalPodAutoscaler
# metadata:
#   name: o2obots-new-frontend-hpa
#   namespace: application
# spec:
#   scaleTargetRef:
#     apiVersion: apps/v1
#     kind: Deployment
#     name: o2obots-new-frontend
#   minReplicas: 1
#   maxReplicas: 1
#   metrics:
#     - type: Resource
#       resource:
#         name: cpu
#         target:
#           type: Utilization
#           averageUtilization: 80
#     - type: Resource
#       resource:
#         name: memory
#         target:
#           type: Utilization
#           averageUtilization: 80
